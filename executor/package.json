{
	"private": true,
	"version": "0.0.1",
	"name": "executor",
	"main": "dist/index.js",
	"license": "MIT",
	"scripts": {
		"prebuild": "npm install",
		"build": "ncc build index.ts --minify",
		"prestart": "npm run build",
		"start": "node dist/index.js"
	},
	"dependencies": {
		"@actions/core": "^1.2.2",
		"@actions/exec": "^1.0.3"
	},
	"devDependencies": {
		"@types/node": "^12.12.2",
		"typescript": "^3.7.5"
	}
}
