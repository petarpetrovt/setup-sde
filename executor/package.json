{
	"private": true,
	"version": "0.0.0",
	"name": "executor",
	"main": "index.js",
	"license": "MIT",
	"scripts": {
		"prebuild": "npm install | npm install -g @zeit/ncc",
		"build": "ncc build index.ts --minify --out ./ --no-cache",
		"prestart": "npm run build",
		"start": "node index.js"
	},
	"dependencies": {
		"@actions/core": "^1.2.2",
		"@actions/exec": "^1.0.3"
	},
	"devDependencies": {
		"@types/node": "^12.12.2",
		"typescript": "^3.7.5"
	}
}